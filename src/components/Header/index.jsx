import { HeaderContainer } from "./Header.styles";
import NavBar from "../Navbar";

const Header = () => {
  return (
    <HeaderContainer>
      <NavBar />
    </HeaderContainer>
  );
};

export default Header;
